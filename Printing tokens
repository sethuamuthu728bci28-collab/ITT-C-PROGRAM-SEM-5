#include <stdio.h>
#include <string.h>
int main() {
   char s[1000];
    if (scanf("%[^\n]%*c", s) != 1) {
        return 0;
    }
    char *word = strtok(s, " ");
    while (word != NULL) {
        printf("%s\n", word);
        word = strtok(NULL, " ");
    }
    return 0;
}
